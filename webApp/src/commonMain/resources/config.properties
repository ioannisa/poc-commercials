# Web client config. Served alongside the HTML from the web root and fetched
# at app startup. Edit the deployed copy to change endpoints — no rebuild needed.
#
# The client only needs to know the application-server URL; MySQL lives in
# the server's own config.properties.

server.baseUrl=http://localhost:8080
