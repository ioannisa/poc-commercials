*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
.idea
.DS_Store
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings
node_modules/

# Desktop installers the server hosts at /downloads (auto-update). Per-deployment
# artifacts, often 100+ MB - built and copied in locally, never source.
downloads/

# Deployment config with plaintext credentials (DB/SMTP/admin/API keys). Each
# deployment keeps its own; copy server.yaml.example to server.yaml to start.
server.yaml

# Galaxy Bridge uploads (client business data - lives on the server, never in git)
galaxy-imports/
